# Protocol

Shared contracts for Rhythm OS, the Dart SDK, and the app.

Use this space for API schemas, server event definitions, settings/profile
schemas, contract tests, and generated client/server types. Runtime
implementations should stay in `os/`, SDK transport code should stay in `sdk/`,
and Flutter UI/state should stay in `app/`.
