name: rhythm_sdk
description: Dart SDK for communicating with Rhythm Lighting servers, Linux appliances, and HA add-ons.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  dio: ^5.9.1
  http: ^1.2.0
  logging: ^1.3.0
  meta: any

dev_dependencies:
  test: ^1.24.0
  lints: ^5.1.0
  mocktail: ^1.0.0
